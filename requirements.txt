# Core runtime (install with: pip install -e .)
pyyaml>=6.0
httpx>=0.27
pydantic>=2.0
pydantic-settings>=2.0
rich>=13.0
jinja2>=3.1
numpy>=1.26
Pillow>=10.0
tenacity>=8.2
python-dotenv>=1.0

# Optional local model backends: pip install -e ".[local]"
# Optional dev tools: pip install -e ".[dev]"